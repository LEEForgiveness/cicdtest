import { Enforcer } from 'casbin';
import { Request, Response } from 'express';
export interface Authorizer {
    checkPermission(): Promise<boolean>;
}
interface AuthzOptions {
    newEnforcer: Promise<Enforcer>;
    authorizer?: Authorizer;
}
export declare function authz(opt: AuthzOptions): (req: Request<import("express-serve-static-core").ParamsDictionary, any, any, import("express-serve-static-core").Query>, res: Response<any>, next: any) => Promise<void>;
export {};
